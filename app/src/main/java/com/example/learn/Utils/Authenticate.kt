package com.example.learn.Utils

import android.content.Context

object Authenticate {
    fun login(context: Context,userName: String, Password: String) {
        //RequestServer.postData(context)
    }
}